import React, {useEffect, useState} from 'react';
import {Container, Card, UserInfo, UserImg} from '../styles/FeedStyles.js';
import {
    View,
    ScrollView,
    Text,
    StyleSheet,
    FlatList,
    SafeAreaView,
    Alert,
    TouchableOpacity,
  } from 'react-native';
  const HomeScreen = ({navigation}) => {
    return (
        <Container>
            <Card>
                <Text>Hello</Text>
            </Card> 
        </Container>
    );
  }

  export default HomeScreen;

